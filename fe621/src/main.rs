use fe621::assn3::*;
//use fe621::yahoo_test::a;

fn main() {
    prob1::a().unwrap();
    // prob1::a();
    // prob1::b();
    // prob1::d();
    // prob2::a();

    // use polars::df;
    // use polars::prelude::*;

    // let df = df![
    //     "a" => [1, 2, 3],
    //     "b" => [None, Some("a"), Some("b")]
    // ]
    // .unwrap();

    // let filtered = df.lazy().filter(col("a").gt(lit(2))).collect().unwrap();
    // println!("{}", filtered);
    // filtered:

    // ╭─────┬─────╮
    // │ a   ┆ b   │
    // │ --- ┆ --- │
    // │ i64 ┆ str │
    // ╞═════╪═════╡
    // │ 3   ┆ "c" │
    // ╰─────┴─────╯
}
